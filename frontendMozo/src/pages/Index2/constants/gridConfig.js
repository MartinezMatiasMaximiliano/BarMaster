// constants/gridConfig.js

// Constantes de configuración del grid
export const GRID_CONFIG = {
    cols: 15,
    rowHeight: 50,
    minWidth: 1,
    minHeight: 1
};

// Valores por defecto para coordenadas de mesas
export const DEFAULT_MESA_COORDS = {
    x: 0,
    y: 0,
    w: 1,
    h: 1
};

